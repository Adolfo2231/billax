{
    "version": "1.0",
    "services": {
        "web": {
            "command": "python -c 'from app import create_app; app = create_app(); app.run(host=\"0.0.0.0\", port=8000)'",
            "port": 8000,
            "image": "default"
        }
    },
    "images": {
        "default": {
            "dockerfile": "Dockerfile",
            "context": "."
        }
    }
} 